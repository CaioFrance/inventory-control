import { Box } from "@mui/system";
import Layout from "../layout/Layout";

export default () => {
  return (
    <Layout>
      <Box>
        <h1>Inventory Page</h1>
      </Box>
    </Layout>
  );
};
